{
  "name": "obvhs-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "cd ../crates/obvhs-wasm && wasm-pack build --target web --out-dir ../../web/src/wasm/pkg"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.4.0",
    "@webgpu/types": "^0.1.52"
  }
}
